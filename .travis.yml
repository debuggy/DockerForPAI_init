language: python

branches:
  only:
    -master

python:
  - "3.6"
# command to install dependencies
# install:
#   - pip install -r requirements.txt
# command to run tests
script:
  - python paitest.py